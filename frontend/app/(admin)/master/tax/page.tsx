import TaxFeature from '@/features/tax';

export const metadata = {
  title: 'Tax Management | A-Flick CRM',
  description: 'Manage tax rates and statuses for various services.',
};

export default function TaxPage() {
  return <TaxFeature />;
}
